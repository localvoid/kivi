language: node_js
sudo: required
dist: trusty
node_js:
  - "6"

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - g++
      - google-chrome-stable

cache:
  directories:
    - node_modules
    - typings

install:
  - ./tools/ci/install.sh

script:
  - ./tools/ci/test.sh
