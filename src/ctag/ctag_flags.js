goog.provide('kivi.CTagFlags');

/**
 * CTag Flags
 *
 * @enum {number}
 */
kivi.CTagFlags = {
  /** [kivi.CTag] is in SVG namespace. */
  SVG:            0x00001,
  /** Clone Element instead of creating */
  ENABLE_CLONING: 0x00002
};
